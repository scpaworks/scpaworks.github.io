language: ruby
cache: bundler
before_install:
  - gem install bundler
  - gem install just-the-docs
script:
  - bundle
branches:
  only:
    # Change this to gh-pages if you're deploying using the gh-pages branch
    - master
rvm:
 - 2.4
